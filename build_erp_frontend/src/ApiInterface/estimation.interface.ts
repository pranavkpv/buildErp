export interface estimationSaveInterface  {
   spec_id: string
   spec_name: string,
   unitrate: number,
   quantity: number,
   total: number
}

export interface reasonInterface {
   reason:string 
   projectId:string
}